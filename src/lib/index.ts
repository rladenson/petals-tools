// place files you want to import through the `$lib` alias in this folder.

export enum OnOffNone {
	None,
	On,
	Off
}

export enum AutoproxyEnum {
	None = 'none',
	Off = 'off',
	Front = 'front',
	Latch = 'latch',
	Member = 'member'
}

export const baseURL = 'https://api.pluralkit.me/v2/';
